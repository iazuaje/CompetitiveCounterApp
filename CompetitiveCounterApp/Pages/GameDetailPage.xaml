<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:f="clr-namespace:Fonts"
             xmlns:pageModels="clr-namespace:CompetitiveCounterApp.PageModels"
             xmlns:models="clr-namespace:CompetitiveCounterApp.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="CompetitiveCounterApp.Pages.GameDetailPage"
             x:DataType="pageModels:GameDetailPageModel"
             Title="{Binding Name}"
             Shell.BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
             Shell.ForegroundColor="{StaticResource White}"
             Shell.TitleColor="{StaticResource White}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Editar"
                     Command="{Binding EditCommand}"
                     IconImageSource="{StaticResource IconEdit}" />
        <ToolbarItem Text="Eliminar"
                     Command="{Binding DeleteCommand}"
                     IconImageSource="{StaticResource IconDelete}" />
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="0.2*, auto" BackgroundColor="{Binding Game.CurrentGameColor}">
        <Image Source="{Binding Game.GameImage}" Aspect="AspectFill" HeightRequest="500" />
        <VerticalStackLayout Spacing="10" HorizontalOptions="Center" VerticalOptions="Center" Margin="10">
            <Border Background="{AppThemeBinding Light={StaticResource LightBackground}, Dark={StaticResource DarkBackground}}"
                    StrokeThickness="2"
                    StrokeShape="RoundRectangle 50"
                    Padding="40,10"
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource PrimaryDark}}">
                <Border.Shadow>
                    <Shadow Offset="0,4" Radius="8" Opacity="0.5"/>
                </Border.Shadow>
                <VerticalStackLayout Spacing="5">
                    <Label Text="{Binding Name}"
                           HorizontalTextAlignment="Center"
                           FontSize="20"
                           FontAttributes="Bold" />
                    <BoxView HeightRequest="1" Color="Gray" />
                    <Image>
                        <Image.Source>
                            <FontImageSource Glyph="{Binding SelectedIcon.Icon}"
                                             FontFamily="{Static f:FluentUI.FontFamily}"
                                             Color="{Binding Game.CurrentGameColor}"
                                             Size="40" />
                        </Image.Source>
                    </Image>
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
        <Border Grid.Row="1"
                Background="{AppThemeBinding Light={StaticResource LightBackground}, Dark={StaticResource DarkBackground}}"
                Padding="10">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="30,30,0,0" />
            </Border.StrokeShape>
            <Border.Shadow>
                <Shadow Offset="0,-10" Radius="10" Opacity="0.2"/>
            </Border.Shadow>
            <ScrollView>
                <VerticalStackLayout Spacing="15">
                    <Border>
                        <VerticalStackLayout Spacing="10">
                            <Label Text="Descripción"
                                   FontSize="16"
                                   FontAttributes="Bold" />
                            <Label Text="{Binding Description}"
                                   FontSize="14"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                        </VerticalStackLayout>
                    </Border>

                    <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                        <Label Text="Sesiones de Juego"
                               FontSize="18"
                               FontAttributes="Bold"
                               VerticalOptions="Center"
                               HorizontalOptions="Start" />
                        <Button Text="+ Nueva Sesión"
                                Command="{Binding AddSessionCommand}"
                                BackgroundColor="{Binding Game.CurrentGameColor}"
                                TextColor="White"
                                Padding="10,5"
                                FontSize="14"
                                CornerRadius="8" />
                    </HorizontalStackLayout>

                    <VerticalStackLayout Spacing="10"
                                         BindableLayout.ItemsSource="{Binding Sessions}">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="models:Session">
                                <Border BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}"
                                        StrokeThickness="0"
                                        Padding="15">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="8" />
                                    </Border.StrokeShape>
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding BindingContext.NavigateToSessionCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}"
                                                              CommandParameter="{Binding .}" />
                                    </Border.GestureRecognizers>

                                    <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="5">
                                        <Label Grid.Row="0"
                                               FontSize="16"
                                               FontAttributes="Bold">
                                            <Label.Text>
                                                <MultiBinding StringFormat="{}{0:dd/MM/yyyy HH:mm}">
                                                    <Binding Path="SessionDate" />
                                                </MultiBinding>
                                            </Label.Text>
                                        </Label>

                                        <Label Grid.Row="1"
                                               FontSize="14"
                                               TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}">
                                            <Label.Text>
                                                <MultiBinding StringFormat="{}{0} jugador(es)">
                                                    <Binding Path="SessionPlayers.Count" />
                                                </MultiBinding>
                                            </Label.Text>
                                        </Label>

                                        <Label Grid.Row="2"
                                               Text="{Binding Notes}"
                                               FontSize="12"
                                               TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray500}}"
                                               LineBreakMode="TailTruncation"
                                               MaxLines="1" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                        <BindableLayout.EmptyView>
                            <Label Text="No hay sesiones registradas aún"
                                   FontSize="14"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"
                                   HorizontalOptions="Center"
                                   Margin="0,20" />
                        </BindableLayout.EmptyView>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </ScrollView>
        </Border>
    </Grid>

</ContentPage>